query ReleaseQuery($id: UUID!) {
    release(id: $id) {
        id,
        gitlabId,
        gitHubId,
        gitlabIid,
        date,
        state,
        title,
        notes,
        createdAt,
        lastModifiedAt,
    }
}